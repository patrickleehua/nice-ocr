DATABASE_URL="file:./dev.db"
STORAGE_DIR="./storage"

# Provider API Key 静态加密主密钥（base64 的 32 字节）。
# 生成：openssl rand -base64 32
# 设置后保存的 provider Key 会以 AES-256-GCM 加密入库；存量明文用 `pnpm db:encrypt-secrets` 迁移。
PROVIDER_KEY_ENCRYPTION_KEY=""
